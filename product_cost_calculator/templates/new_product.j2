{% extends 'base.j2' %}
{% block content %}
  <div class="container col-3 mt-2">
    <legend class="h2">New Product</legend>
    <form method="POST" action="">
      {{ form.hidden_tag() }}
      <div class="form-group">
        {{ form.name.label(class='font-weight-bold')}}
        {% if form.name.errors %}
          {{ form.name(class='form-control is-invalid') }}
          <div class="invalid-feedback">
            {% for error in form.name.errors %}
              {{ error }}
            {% endfor %}
          </div>
        {% else %}
          {{ form.name(class='form-control') }}
        {% endif %}
      </div>
      <div class="form-group">
        {{ form.amount.label(class='font-weight-bold')}}
        {% if form.amount.errors %}
          {{ form.amount(class='form-control is-invalid') }}
          <div class="invalid-feedback">
            {% for error in form.amount.errors %}
              {{ error }}
            {% endfor %}
          </div>
        {% else %}
          {{ form.amount(class='form-control') }}
        {% endif %}
      </div>
      <div class="form-group">
        {{ form.unit.label(class='font-weight-bold')}}
        {% if form.unit.errors %}
          {{ form.unit(class='form-control is-invalid') }}
          <div class="invalid-feedback">
            {% for error in form.unit.errors %}
              {{ error }}
            {% endfor %}
          </div>
        {% else %}
          {{ form.unit(class='form-control') }}
        {% endif %}
      </div>
      {{ form.public }}
      {{ form.public.label }}
      <div class="form-group">
        {{ form.submit(class='btn btn-outline-primary')}}
      </div>
    </form>
  </div>
{% endblock %}